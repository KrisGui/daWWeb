import React, {
  // useState,
  // useContext
} from 'react';
// import { ToneCtx } from '../../Context/_ToneContext'
import {
  // Select,
  Button
} from '../UI';

const Controller = (props) => {
  // const toneCtx = useContext(ToneCtx)
  // const {
  //   trackTypes,
  //   instrumentTypes,
  //   effectTypes
  // } = toneCtx;
  return (
    <div>
      {/* <Select options={trackTypes} defaultValue={'Select Track Type'}/>
      <Select options={instrumentTypes} defaultValue={'Select Device'}/> */}
      <Button
        buttonText={'Add track'}
        // onClick={() => }
        />
    </div>
  )
}

export default Controller;
